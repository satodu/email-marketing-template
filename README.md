# email-marketing-template
Using MJML for build the email marketing
  - For start the project install the dependencies:
    npm install
  - Developing and using the task I recommend:
    ./node_modules/.bin/mjml --watch index.mjml -o index.html
  - Some elements are avaliable in folder elements and included in mjml index file.
